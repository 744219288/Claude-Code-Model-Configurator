# V2.9.2 国内网络增强升级报告

日期：2026-08-23

## 升级目标

让国内普通用户在全新 Windows 10/11 x64 电脑、没有 Node.js/npm/Python/VS Code 的情况下，也能通过配置器完成 Claude Code + DeepSeek 核心部署；任一在线来源失败后提供可选择的替代方案，而不是只显示阻断式错误。

## 已完成

1. 内置 Node.js 22.23.2 Windows x64 便携包，构建时核对 Node.js 官方 `SHASUMS256.txt`；运行时验证 `node.exe` 的 OpenJS Foundation Authenticode 签名。
2. 受管理 Node/npm 解压到 `%LOCALAPPDATA%\ClaudeDeepSeekConfigurator`，不写系统 PATH、不覆盖用户 Node；卸载时完整移除。
3. Claude Code 支持 npm 国内镜像、npm 官方源、Anthropic 原生安装器和 WinGet 四条路径；安装前并行检测实际端点并按延迟/可用性选路。
4. npm 镜像使用命令级 `--registry`，不会永久修改用户 `.npmrc`；显式包含平台可选依赖并在安装后执行 `claude --version`。
5. WinGet 固定 `winget` 源，启用非交互、详细日志和可选代理；返回代码与脱敏输出进入安装诊断。
6. 安装来源全部失败时显示引导式方案窗口，可选择国内镜像、npm 官方、Anthropic 官方、WinGet、检测到的代理或导出诊断。
7. VS Code与Anthropic扩展改为可选增强，默认不安装；失败不再阻断CLI核心功能。
8. 新增安全更新中心。用户原有组件只显示版本；配置器管理的Claude Code可沿原来源更新，失败时尝试恢复原版本。配置器自更新框架要求HTTPS、SHA-256和指定发布者数字签名。
9. 受管理Node/npm、npm缓存、更新状态和安装尝试均纳入所有权日志、脱敏诊断与完整卸载。
10. 桌面快捷方式改为打开配置器主界面，不再直接打开 Claude Code 终端；终端启动保留为配置器内的明确按钮。

## 验证结果

- 自动化回归：87项全部通过，其中包含安装中断后受管理 Node/npm、Claude 前缀和 npm 缓存的清理回归测试。
- Node离线资源：官方SHA-256匹配。
- Node运行文件：Windows Authenticode有效，发布者为OpenJS Foundation。
- 受管理Node/npm：真实解压并执行 `node --version`、`npm --version` 成功。
- 国内核心路径：在隔离临时目录强制隐藏本机Claude、Node和npm后，成功通过 `registry.npmmirror.com` 安装受管理Claude Code，并通过 `claude --version` 验证。
- 隔离测试结束后删除临时目录，未写系统PATH、未安装系统Node。

## 尚未宣称完成的验证

- 仍需在至少一台真正全新Windows 10和Windows 11 x64虚拟机进行完整GUI安装、DeepSeek连接、桌面启动、更新、卸载和安装前后快照对比。
- 仍需在中国电信、联通、移动及单位代理网络分别抽样测试。
- 测试包尚未代码签名，不适合直接作为普通用户正式版公开发布。
- Claude Code二进制没有直接内置；在再分发许可确认前，完全断网且电脑原本没有Claude Code时无法完成Claude本体安装。

## 发布边界

本版本是未签名测试版。正式发布前必须完成配置器代码签名、配置正式HTTPS更新清单地址，并对Claude Code再分发与中国大陆使用相关条款进行确认。
