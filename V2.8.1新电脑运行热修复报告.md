# V2.8.1 新电脑运行热修复报告

日期：2026-08-22

## 结论

截图中的 `[WinError 5] 拒绝访问` 已修复。V2.8.1 不再从 `%TEMP%` 直接启动 Python 或 VS Code 安装器，而是先放入 `%LOCALAPPDATA%\ClaudeDeepSeekConfigurator\installers`，完成数字签名和发布者验证后执行。

截图采用旧版顶部横幅界面，不是当前左侧紫蓝品牌栏界面。新构建的窗口标题和左侧标签均显示 `V2.8.1`，可用来确认测试机没有误用旧 EXE。

## 本轮实测发现并修复的问题

### 1. 临时目录拒绝执行

- 原因：部分 Windows 安全策略、AppLocker、杀毒软件或企业应用控制会禁止从 `%TEMP%` 启动 EXE。
- 修复：Python 3.12 与 VS Code 安装器迁移到应用专属 `installers` 目录；离线 Python 安装器采用原子复制。
- 保留：Python Software Foundation 与 Microsoft Authenticode 发布者验证。

### 2. Python 安装器返回成功但运行时不可用

- 增加标准用户目录、Program Files、`py` 启动器、PATH 和 Python 注册表位置的 3.12 发现。
- Python 官方安装器写入 `%LOCALAPPDATA%\ClaudeDeepSeekConfigurator\python-install.log`。
- 安装后仍不可用时提示修复或卸载电脑上损坏的旧 Python 3.12。
- Python 安装日志加入“导出诊断”ZIP。

### 3. LiteLLM 代理无法安全停止

- 实测发现 `litellm.exe` 启动器 PID 与真正监听 4000 端口的 Python 子进程 PID 不一致。
- V2.8.1 分别记录启动器 PID 和监听 PID；停止前核对实际监听 PID，再终止已验证的启动器进程树。
- 修复后代理鉴权通过、停止成功，4000 端口不再监听。

## 实际干净目录部署结果

测试目录从不存在状态开始，未复用任何旧 venv：

- 102 个离线 wheel 实际安装成功。
- `litellm==1.80.11`、FastAPI、Starlette、Uvicorn、Pydantic 导入成功。
- `pip check` 通过。
- LiteLLM 在 `127.0.0.1:4000` 启动成功。
- 随机令牌鉴权健康检查通过。
- 代理进程树停止成功；4000 端口无监听残留。
- 第二次运行正确复用 Python/venv，复用检查耗时 5.5 秒。
- 完整部署验证耗时 137.7 秒。
- 66/66 项自动化测试通过。

机器上没有可用的 Windows Sandbox，当前账户也没有 Hyper-V 虚拟机访问权限，因此本轮不能宣称完成了真正的全新 Windows 虚拟机测试。Claude Code 与 VS Code 的主机级安装使用隔离自动化测试覆盖，避免改动当前电脑现有软件。正式签名发布前仍应在一台有权限的干净 Windows 10/11 x64 虚拟机执行最终端到端测试。

## 新电脑复测方法

1. 不再使用截图中的旧 EXE。
2. 把 V2.8.1 包完整解压到普通文件夹。
3. 启动后确认标题与左侧标签显示 `V2.8.1`。
4. Python 阶段的安装器应位于 `%LOCALAPPDATA%\ClaudeDeepSeekConfigurator\installers`，不应再出现 `%TEMP%\claude-deepseek-*\python312.exe`。
5. 如果 V2.8.1 仍提示 Windows 拒绝执行，查看 Windows 安全中心“保护历史记录”；单位电脑需要管理员确认应用控制策略，配置器不会绕过系统安全策略。
