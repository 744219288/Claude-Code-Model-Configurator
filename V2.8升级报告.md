# Claude Code + DeepSeek 配置器 V2.8 / V2.8.1 升级报告

日期：2026-08-22

## 1. 升级结论

本次以《配置器改进总览与修改指南》为建议清单，对 V2.7 源码逐项复核后升级为 V2.8 安全增强版。V2.7 已经完成的功能没有重复重写；仍有缺口且能够在当前源码中安全落地的项目已经实现。

最终验证结果：

- 66/66 项 Python 自动化测试通过。
- Tkinter GUI 自检通过。
- 5 个 PowerShell 构建/签名/打包脚本通过语法解析。
- 102 个离线 wheel 已生成全量 SHA-256 锁。
- CycloneDX 1.5 SBOM 包含 102 个依赖组件。
- `offline/manifest.json` 当前覆盖 104 个文件。

## 2. 本次实际完成的改进

### 2.1 本机代理鉴权（A1）

- 首次配置时生成随机 `sk-local-*` 代理令牌，不再以固定 `dummy` 作为正常访问令牌。
- LiteLLM `general_settings.master_key` 从 `LITELLM_MASTER_KEY` 子进程环境变量读取，不把令牌写入 YAML。
- Claude Code、VS Code、连接测试和代理健康检查均使用同一随机令牌。
- V2.7 旧代理只在 PID 文件与 4000 端口监听 PID 一致时才会被停止并迁移，避免误杀其他进程。
- 该令牌能阻止不知道令牌的本机调用，但不是抵御同一 Windows 用户下恶意进程的强安全边界；同用户进程仍可能读取用户环境或进程信息。

### 2.2 凭据隔离（A2）

- DeepSeek API Key 继续保存在 `ClaudeDeepSeekConfigurator/DeepSeekApiKey`。
- 新增独立凭据 `ClaudeDeepSeekConfigurator/ProxyMasterKey`。
- DeepSeek Key 仅在启动代理时读取并注入代理子进程，不写入父进程环境、命令行、YAML、状态文件或日志。
- 代理令牌需要提供给 Claude Code，因此保存到当前用户的 `ANTHROPIC_AUTH_TOKEN`；它与可消费余额的 DeepSeek Key 不是同一凭据。

### 2.3 全量依赖哈希锁（A3）

- 新增 `offline/requirements.lock`，固定 102 个依赖的名称、版本和 SHA-256。
- 离线、本地失败后的 PyPI 官方源、TUNA 回退源都使用 pip `--require-hashes`。
- 保留 V2.7 已有的逐 wheel PyPI 官方哈希核对与 manifest 校验，形成“构建时官方哈希 + 发布清单 + 安装时 require-hashes”三层校验。

### 2.4 安装包签名校验（A4）

V2.7 已实现，无需重复修改：Python、VS Code 和 Anthropic 原生 Claude Code 均验证 Authenticode 状态与发布者；发布者无法读取时拒绝执行。

### 2.5 完整卸载（A5）

- GUI 新增“完整卸载”。
- 停止且只停止配置器记录并占用 4000 端口的代理进程。
- 删除私有 Python、venv、配置、日志、PID、终端入口和桌面快捷方式。
- 删除 DeepSeek Key 与代理令牌凭据。
- 从用户 PATH 精确移除配置器 `bin` 目录。
- 首次 V2.8 安装时备份原有网关环境变量；卸载时恢复。
- V2.7 遗留的 `dummy` 等配置不会被误当作用户原始值；用户安装后自行修改的值不会被卸载器覆盖。
- Claude Code 和 VS Code 本体不会被卸载。

### 2.6 诊断、状态与体验（B2、D1、D2）

- GUI 新增“检查状态”：显示代理鉴权、Key、模型、Python/LiteLLM、Claude Code 和 VS Code 状态。
- GUI 新增“导出诊断”：输出 ZIP，包含状态、非敏感 state、离线清单摘要和脱敏代理日志。
- 诊断包明确移除 DeepSeek Key、代理令牌和环境变量备份。
- 模型切换 UI 在 V2.7 已存在，无需重复实现。

### 2.7 SBOM、源码与变更记录（C1、C2）

- 新增 CycloneDX 1.5 `SBOM.cdx.json`，记录 102 个离线依赖及其哈希。
- 保留完整 `installer/` 源码与测试，新增 `CHANGELOG.md`。
- 新增可重复生成 lock、SBOM、manifest 的 PowerShell 5.1 兼容脚本。

### 2.8 发布签名与启动自校验（P0）

- 新增 `sign_build.ps1`：用 Windows SDK `signtool`、证书指纹、SHA-256 和 RFC3161 时间戳签名。
- 签名后生成 `release-integrity.json` 和 `SHA256SUMS`。
- 打包版启动时同时核对自身 SHA-256、Authenticode 有效性和发布者。
- `package_v2.ps1` 会拒绝未签名、哈希不一致、无 SBOM、无 lock 或离线 manifest 为空的正式发布包。
- 发送说明已删除“仍要运行”绕过 SmartScreen 的引导，改为签名或哈希异常时停止运行。

### 2.9 合规与质量说明（C3、C4）

- README、微信发送说明和兼容性说明均明确：这是第三方社区工具，不代表 Anthropic、DeepSeek、Microsoft 或 LiteLLM。
- 明示通过代理让 Claude Code 使用 DeepSeek 可能受相关服务最新条款约束。
- 明示 DeepSeek 与 Claude 在能力、行为、质量和工具兼容性上存在差异。

### 2.10 安装界面、品牌图标与桌面快捷方式

- 安装界面重构为浅色 Fluent 风格双栏布局：左侧集中展示产品定位与安全特性，右侧保留 API Key、模型选择、安装进度和辅助操作。
- 采用 Segoe UI / Microsoft YaHei UI 字体栈、统一紫蓝主色、清晰的主次按钮和确定/不确定两类进度反馈。
- 增加 Windows 高 DPI 适配、窗口居中、最小尺寸约束和 Windows 11 圆角窗口提示；在常见 125% 缩放下完成截图验收，无内容裁切。
- 新增原创紫蓝终端/安全节点图标，提供 1024px PNG 和含 16、20、24、32、40、48、64、128、256px 图层的 ICO。
- 图标已用于 Tkinter 窗口、PyInstaller EXE 和桌面快捷方式；打包版首次安装成功后自动创建桌面快捷方式。
- 新增两项自动化测试，分别验证品牌图标资源存在，以及快捷方式显式使用 EXE 内嵌图标。

## 3. V2.7 已完善、因此未重复修改的项目

- 真实离线 Python 与 wheelhouse、非空 manifest、逐文件大小与 SHA-256 校验。
- Python、VS Code、Claude Code 下载签名与发布者校验。
- 原子下载、三次重试、Content-Length 容错与失败换源。
- 私有 Python 3.12、损坏 venv 重建、ensurepip 修复、pip check。
- 安装中断后的组件探测与复用。它不是逐步骤事务状态机，但已经达到可安全重跑与断点式恢复的目标。
- 原生 `claude.exe` 优先、安全 `claude.cmd`、PowerShell ExecutionPolicy 不放宽。
- `deepseek-chat` / `deepseek-reasoner` 模型选择与小模型固定逻辑。
- 52 项可运行测试源码；V2.8.1 最终扩展为 66 项。

## 4. 未自动完成或保留为后续决策的项目

| 项目 | 当前处理 | 原因 |
|---|---|---|
| 正式 EXE 代码签名 | 流程已实现，尚未产生正式签名 EXE | 当前机器没有 `signtool`、代码签名证书或证书发布者信息；不能伪造作者身份 |
| LICENSE | 未指定 | 许可证选择会授予法律权利，必须由作者决定；当前文档明确“提供源码不等于已开源授权” |
| 自定义 DeepSeek base URL | 未加入普通用户 UI | 任意地址会把 API Key 发送给第三方服务器，安全风险高；系统 HTTPS_PROXY 仍受支持 |
| 自更新器 | 未实现 | 尚无固定发布 URL、签名公钥/证书策略和回滚渠道；贸然实现会扩大供应链风险 |
| 真便携版 | 未实现 | 当前仍依赖用户凭据管理器、用户环境和私有运行目录 |
| WSL/macOS/Linux | 未实现 | 当前架构包含 WinTrust、Credential Manager、注册表、CMD 和 Windows 安装器 |
| GitHub Actions CI | 未创建 | 当前目录不是 Git 仓库，也没有代码签名 secrets 与发布仓库；本地可复现脚本已就绪 |

## 5. 正式发布步骤

在干净的 Windows 10/11 x64 构建机上：

```powershell
powershell -ExecutionPolicy Bypass -File .\build_offline_assets.ps1
powershell -ExecutionPolicy Bypass -File .\build.ps1
powershell -ExecutionPolicy Bypass -File .\sign_build.ps1 -CertificateThumbprint '<代码签名证书指纹>' -ExpectedPublisher '<证书发布者名称>'
powershell -ExecutionPolicy Bypass -File .\package_v2.ps1
```

正式发送前还应在全新 Windows 10 与 Windows 11 x64 虚拟机各完成一次：安装、连接、终端启动、VS Code 启动、诊断导出、升级覆盖和完整卸载测试。

## 6. 验证备注

V2.7 基线测试首次运行时有 51 项通过、1 项因当前 Codex 内置 Python 错误解码中文工作目录而出现路径字符串比较失败；PowerShell 实际解析到了正确的 `safe/claude.cmd`。测试已改为验证命令名及父目录优先级，避免把执行环境乱码误判为产品故障。V2.8.1 增加新电脑安装路径、Python 发现和代理进程树测试后，66 项全部通过；另完成一次 102 wheel 干净目录真实部署。

## 7. 当前交付物状态

- `Claude-Code-DeepSeek-Configurator-V2.8.1-Secure-Source.zip`：V2.8.1 可审计源码包。
- `assets/app_icon.png` / `assets/app_icon.ico`：原创应用图标源文件与 Windows 多尺寸图标。
- `dist/Claude-Code-DeepSeek-一键配置器.exe`：含 V2.8.1 新电脑热修复、新版界面和图标的未签名测试构建，GUI 自检退出码 0，SHA-256 为 `F70799D80DC2229FC56A997665D20123059799E6FE5E669FA73258018F532ED8`；不得作为正式发送包。
- 正式签名 EXE/微信发送包：等待作者提供代码签名证书并执行第 5 节流程。
