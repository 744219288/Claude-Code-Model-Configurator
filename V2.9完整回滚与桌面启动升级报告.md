# V2.9 完整回滚与桌面启动升级报告

> V2.9.2 行为更新：本文记录的 V2.9 桌面直达终端方案已取消。V2.9.2 起桌面快捷方式改为打开配置器主界面，用户再点击界面内的“启动 Claude Code”。

日期：2026-08-22

## 用户目标

1. 卸载后尽可能恢复到从未运行配置器之前的状态。
2. 安装前已有的 Python、Claude Code、VS Code 和 Anthropic 扩展必须保留。
3. 由配置器新增的上述组件必须全部删除。
4. 安装成功后桌面立即出现可直接启动 Claude Code 的品牌快捷方式。

## 安装所有权

V2.9 在保存 API Key、安装 Python 或下载外部软件之前创建原子回滚日志，记录：

- Python 3.12 是否存在及原路径；
- Claude Code 是否存在、原路径及原生/npm 标记；
- VS Code 是否存在、原路径和用户数据目录状态；
- `anthropic.claude-code` 扩展是否存在；
- 相关 Claude/VS Code 用户目录是否在安装前存在；
- 当前用户 PATH 基线；
- 同名桌面快捷方式及备份。

每个组件安装成功后立即记录实际安装方式和路径。卸载步骤成功后写入 `rolled_back`，中途失败可以安全重试。

## 完整回滚行为

- Python：仅当由配置器安装时，调用保存的 Python 官方安装程序 `/uninstall /quiet`，清理程序文件和 Windows 安装记录。
- Claude Code：按 npm、WinGet 或 Anthropic 原生安装方式撤销；原生安装仅删除安装前不存在的 Claude 文件和数据目录。
- VS Code：按 WinGet 或微软官方 `unins000.exe` 撤销；只在安装前不存在时删除 VS Code 用户数据目录。
- Anthropic 扩展：仅当配置器新增时执行 `code --uninstall-extension anthropic.claude-code`。
- PATH：只移除基线之后由受管组件增加的精确路径，不影响用户同期添加的其他路径。
- 配置：停止本地代理，恢复环境变量，删除凭据、终端入口、LiteLLM、独立环境、日志、桌面快捷方式和配置目录。
- 后台自删除：最多重试 30 次并检查结果；失败时向用户显示明确提示。

## 桌面启动入口

旧版未生成桌面图标的原因是将路径参数错误地追加在 `powershell.exe -Command` 后，Windows PowerShell 把路径解析为命令文本；返回值又被主流程忽略。

V2.9 改为通过专用环境变量传递 `.lnk` 路径、目标和参数，并把创建失败作为安装错误。最终快捷方式：

- 名称：`Claude Code + DeepSeek.lnk`
- 目标：`%LOCALAPPDATA%\ClaudeDeepSeekConfigurator\ClaudeDeepSeekConfigurator.exe`
- 参数：`--launch-claude`
- 图标：配置器 EXE 内嵌品牌图标
- 行为：静默检查/启动 LiteLLM，然后在 Windows Terminal 或 UTF-8 CMD 中打开 Claude Code。

## 验证

- 73 项自动化测试全部通过。
- 真实 Windows `.lnk` 创建成功。
- COM 回读确认目标 EXE、`--launch-claude` 参数和图标路径正确。
- 既有组件保护、Python 官方卸载、npm Claude 回滚、Anthropic 扩展卸载、后台清理无窗口参数均有回归测试。

## 尚需完成

在正式公开发布前，必须在真正干净的 Windows 10/11 x64 虚拟机完成一次 V2.9 安装、桌面启动、实际使用、完整回滚和安装前/卸载后快照对比，并对最终 EXE 完成 Authenticode 代码签名。
