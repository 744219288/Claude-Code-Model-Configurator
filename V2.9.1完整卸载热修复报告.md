# V2.9.1 完整卸载热修复报告

日期：2026-08-22

## 真实故障结论

测试机运行 V2.9.0 时出现 `Python 3.12 安装失败（代码 1603）`。诊断日志确认旧版卸载只删除了 `%LOCALAPPDATA%\ClaudeDeepSeekConfigurator\python312` 文件，却保留了 Python 3.12.10 的 Windows 安装登记。新安装器因此进入 `Modify/Updating`，尝试调整旧文档组件 `doc_JustForMe` 时返回 `0x80070643/1603`。

这同时证明旧版“完整卸载”没有达到恢复运行前状态。

## 本版修复

1. 安装前基线同时记录可运行 Python、PythonCore InstallPath 和 Windows“已安装的应用”产品登记。
2. Python、Claude Code、VS Code、Anthropic 扩展和桌面入口全部在实际安装前写入 `pending` 所有权，安装中断后仍可卸载。
3. 环境变量原值在写入注册表前原子保存，不再等到全部安装成功。
4. 重复运行继承首次安装所有权，防止配置器安装的组件被重新标记为用户原有。
5. 旧版指向配置器专属目录的 Python 登记由官方安装器先 `/repair`；无法修复时安全 `/uninstall` 后重新安装。
6. Python 卸载失败会自动官方修复并重试；最终同时验证 InstallPath、产品登记和私有运行目录均已消失。
7. 卸载逐项验证凭据、环境变量和桌面入口；仍有配置器持久内容时抛出错误，不显示完成。
8. 任何可运行的现有 npm 或原生 Claude Code 都直接复用，避免 2.1.239 被切换到 2.1.231。
9. 诊断包纳入 Python 安装、修复、旧登记撤销和卸载日志，并继续脱敏 API Key 与代理令牌。
10. 旧版没有所有权日志的 Claude Code、VS Code 和扩展会显示单独确认；用户确认测试机原先没有这些组件后，可纳入彻底清理。

## 卸载范围

V2.9.1 可撤销由本版新增的 Python 及 Windows 登记、Claude Code、VS Code、Anthropic 扩展、LiteLLM、虚拟环境、代理进程、配置与日志、凭据、环境变量、PATH 项、稳定配置器副本和桌面快捷方式。安装前已有的同类组件和用户后来手工修改的环境值保留。

旧版无所有权日志时，自动接管能够由配置器专属路径证明来源的内容，例如指向 `%LOCALAPPDATA%\ClaudeDeepSeekConfigurator` 的 Python 登记。来源不明的外部 Claude Code、VS Code 或扩展不会静默删除；卸载界面会列出它们，只有用户确认电脑原先没有这些组件后才纳入彻底清理。

用户下载或解压的原始 ZIP/测试包不是运行安装器产生的内容，不由卸载按钮删除。Windows 事件日志、Defender 历史、Prefetch 等系统审计记录也不会被破坏。

## 当前验证

- 80 项自动化测试全部通过。
- 覆盖安装前 pending 日志、重复运行所有权继承、旧 Python 登记修复、首次卸载失败后的修复重试、只剩产品登记时继续卸载、已有 npm Claude 复用和外部组件保护。
- 仍需在专用干净 Windows 10/11 x64 虚拟机做安装前后快照对比。
- 仍需在提供诊断包的测试机验证 V2.9.1 能修复并卸载该真实旧登记。
- 当前测试包未做 Authenticode 签名，不可作为普通用户正式公开版。
