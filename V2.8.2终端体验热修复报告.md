# V2.8.2 终端体验热修复报告

日期：2026-08-22

## 修复结果

V2.8.2 修复了在终端输入 `claude` 时，LiteLLM 后台代理可能先弹出空白控制台窗口的问题。Claude Code 仍然在用户当前终端中运行，本地代理在后台静默启动。

同时优化已经安装过 Claude Code 与 VS Code 扩展的电脑：检测到 `anthropic.claude-code` 已在 VS Code 活动扩展注册表中时直接复用，不再通过 `--force` 强制更新。

## 技术调整

- LiteLLM 后台进程使用 `CREATE_NO_WINDOW | CREATE_NEW_PROCESS_GROUP`。
- 使用 `STARTF_USESHOWWINDOW + SW_HIDE` 作为 Windows 隐藏窗口兜底。
- 移除与 `CREATE_NO_WINDOW` 组合时会导致隐藏行为不可靠的 `DETACHED_PROCESS`。
- 保留标准输出重定向、代理日志、独立进程组、PID/监听 PID 记录和安全停止能力。
- VS Code 扩展检查优先读取 `.vscode/extensions/extensions.json`，并排除 `.obsolete` 中已经废弃的旧扩展目录。
- 只有缺少 Anthropic 扩展时才执行安装，不再使用 `--force`。

## 验证结果

- 68 项自动化测试全部通过。
- 后台启动参数测试确认包含 `CREATE_NO_WINDOW`、`CREATE_NEW_PROCESS_GROUP` 和 `SW_HIDE`。
- 测试确认启动参数不再包含 `DETACHED_PROCESS`。
- 测试确认已有扩展时不会调用 VS Code 安装命令。
- 测试确认已废弃的旧扩展目录不会被误判为有效安装。

## 发布说明

当前生成的是未签名测试构建，仅用于受控测试。正式面向普通用户发布前仍需使用可信代码签名证书完成 Authenticode 签名、时间戳和签名后 SHA-256 完整性记录，并在干净 Windows 10/11 x64 电脑上复测最终签名包。
