# V3.0.0 多模型改造报告

## 本次已经做成什么

V2.9.6 原来只认识 DeepSeek。V3.0.0 开发版把“安装 Claude Code”和“选择模型服务”拆开了：Claude Code、Git、Node.js 仍只安装一次，客户以后切换模型厂商时只需选择厂商、选择模型、输入该厂商的 Key，再应用配置。

首批支持 DeepSeek、智谱 GLM、MiniMax、阿里云百炼。百炼按 Coding Plan、Token Plan、按量付费拆成三个入口，避免客户拿错套餐 Key 后收到难理解的鉴权错误。

## 客户能得到什么

- 同一台电脑可保存多个厂商的 Key，彼此隔离，不覆盖、不串用。
- 界面会联动厂商与模型，不能把智谱模型错误地发给 MiniMax。
- 每家厂商使用自己的官方接口、模型名和上下文设置。
- 切换模型不需要卸载或重复下载 Claude Code。
- 状态页会显示当前厂商、接口和模型，出问题时更容易判断。
- 诊断包继续排除所有厂商的 API Key。

## 代码结构变化

- `installer/providers.py`：厂商与模型注册表，是以后扩展的唯一事实来源。
- `installer/credentials.py`：按厂商生成独立的 Windows 凭据目标；DeepSeek 继续读取 V2.9.6 的旧目标以兼容升级。
- `installer/core.py`：连接测试、启动 Claude、启动 VS Code、状态、卸载和安装流程都改为读取当前厂商。
- `installer/gui.py`：增加厂商和模型下拉框，并根据选择自动加载对应 Key。
- `tests/test_providers.py`：验证厂商配置完整、Key 隔离、跨厂商模型拒绝，以及请求地址和模型名正确。

## 安全边界

本版仍坚持直连：没有引入 LiteLLM，也没有在本机开放代理端口。只收录厂商官方文档明确提供的 Anthropic 兼容地址。用户不能手填任意接口地址或任意模型名，目的是减少钓鱼接口、误配置和支持成本。

## 已验证结果

- 103 项自动化测试通过。
- Python 编译检查通过。
- 多厂商连接请求构造测试通过：接口、鉴权头、API 模型名均取自所选厂商。
- GUI 源码和事件逻辑已完成；打包后的控制台调试版和正式窗口版都已通过 Tk 启动自检。Codex 沙箱要求把单文件临时解包目录设在工作区，真实用户环境不受这一沙箱限制。发布前仍应在全新 Windows 电脑上执行人工切换测试。

## 下一步建议

先不要继续堆更多厂商。应先在真实账户上分别完成一次最小对话测试，并确认每个套餐的 Key 类型、余额错误和无模型权限错误都能给客户清楚提示。通过后再生成未签名测试包，最后签名发布。
