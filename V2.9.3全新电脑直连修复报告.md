# V2.9.3 全新电脑直连修复报告

## 交叉验证结论

两张故障截图对应两个不同根因：

1. “删除凭据失败，Windows 错误码 0”不是 Windows 真正返回了成功，而是旧实现通过 `ctypes.windll` 调用凭据 API，没有可靠保留线程最后错误码；API 返回失败后再读取错误码时拿到了 0。
2. “安装包未包含受管理 Node.js”并不代表测试 ZIP 中没有 Node。旧 EXE 从微信临时目录/单文件稳定副本运行后，资源根目录跟随当前 EXE；稳定副本只复制了 EXE，没有复制 `offline`，所以运行时找不到 ZIP 里实际存在的 Node 文件。

原文档提出的“固定资源根目录、错误码正确捕获、完整资源复制”方向成立，但仅修这两处仍不足以保证全新电脑安装。Claude Code 在原生 Windows 还需要 Git Bash；旧版还依赖 Python + LiteLLM + 本地代理，并受 LiteLLM 漏洞、Python 安装登记和 4000 端口等额外故障影响；npm `@latest` 又可能在主包先发布、Windows 平台包尚未同步时安装失败。

## 已落地方案

- 凭据模块改用 `ctypes.WinDLL("Advapi32.dll", use_last_error=True)`，每次调用前清零，失败后立即读取；仅把 `ERROR_NOT_FOUND (1168)` 当作幂等删除成功，访问拒绝等错误保留真实代码。
- 冻结构建首次运行时校验整个相邻 `offline` 清单，复制到 `%LOCALAPPDATA%\Programs\ClaudeDeepSeekConfigurator\offline.new-*`，复制后再次逐文件校验，再原子切换为稳定目录。
- 稳定 EXE、完整离线资源和完整性 sidecar 位于同一程序根目录。桌面与终端入口不再依赖原 ZIP、微信缓存或启动时工作目录。
- 使用 DeepSeek 官方 Anthropic 接口直连，删除新安装流程中的 Python、venv、LiteLLM 和本地代理。
- 内置 PortableGit 2.55.0.5 与 Node.js 22.23.2，均隔离在配置器目录，不要求管理员，不污染系统 PATH。
- Claude 安装顺序优先 WinGet/Anthropic 原生安装，npm 为可靠回退。npm 回退解析 `stable`/`latest` 标签，并逐个确认 `@anthropic-ai/claude-code-win32-x64@同版本` 已发布，只安装同步版本。
- 新模型使用 `deepseek-v4-flash` 和 `deepseek-v4-pro[1m]`，清理旧版本遗留的持久代理环境变量；密钥只在子进程环境中出现。

## 验证证据

- 98 项测试通过。
- PortableGit 官方资产 SHA-256：`5aa8a20f6e9abb2c755f0e73c91c687701a46b309ad84a0ca6509380fa4ae290`；Windows 签名有效，发布者为 Johannes Schindelin。
- Node ZIP 与 Node 官方 SHASUMS256 匹配；实际运行 Node v22.23.2、npm 10.9.8。
- 国内 npm 镜像真实解析同步版本 2.1.231，隔离安装成功，`claude --version` 成功。

## 仍不能虚假保证的边界

没有软件能保证在“任何电脑”绝不失败。企业 AppLocker/WDAC、杀毒误报、网络封锁、磁盘或权限损坏、DeepSeek 服务端故障和账户问题不受安装器控制。V2.9.3 的目标是：在正常 Windows 10/11 x64、当前用户目录可写、至少一个安装源和 DeepSeek 可访问的前提下，不依赖预装开发环境；已知故障要自动回退或给出真实、可定位的错误，而不是错误码 0 或空壳提示。
